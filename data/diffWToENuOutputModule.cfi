include "Configuration/EventContent/data/EventContent.cff"   

    module diffWToENuOutputModule = PoolOutputModule
    {
        untracked string fileName = "diffWToENuFiltered.root"
        using AODSIMEventContent
	untracked PSet SelectEvents = {
                vstring SelectEvents = {"diffWToENuHLTPath"}
        }
        untracked PSet datasets ={
    		untracked string dataTier = "USER"
   		untracked string filterName = "diffWToENu"
    }
}
