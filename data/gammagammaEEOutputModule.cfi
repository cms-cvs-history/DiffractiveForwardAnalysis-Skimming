include "Configuration/EventContent/data/EventContent.cff"   

    module gammagammaEEOutputModule = PoolOutputModule
    {
        untracked string fileName = "gammagammaEEFiltered.root"
        using AODSIMEventContent
	untracked PSet SelectEvents = {
		vstring SelectEvents = {"gammagammaEEHLTPath"}
	}
        untracked PSet dataset ={
    		untracked string dataTier = "USER"
   		untracked string filterName = "gammagammaEE"
    }
}
