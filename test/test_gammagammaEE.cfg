process TestGammaGammaEESkim= {
  include "FWCore/MessageLogger/data/MessageLogger.cfi" 
  include "DiffractiveForwardAnalysis/Skimming/data/gammagammaEE_SkimPaths.cff"
  include "DiffractiveForwardAnalysis/Skimming/data/gammagammaEEOutputModule.cfi"
  replace gammagammaEEOutputModule.fileName = "gammagammaEE.root"

  untracked PSet options = {
    untracked bool wantSummary = true
  }

  untracked PSet maxEvents = { 
    untracked int32 input = 100 
  }

  source = PoolSource {
    untracked vstring fileNames = {
      "file:events.root"
    }
  }

  module evtInfo = AsciiOutputModule {
  }

  endpath end = { 
    evtInfo, gammagammaEEOutputModule 
  }
}
