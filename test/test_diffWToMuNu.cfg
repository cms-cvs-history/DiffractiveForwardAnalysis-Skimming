process TestDiffWToMuNuSkim= {
  include "FWCore/MessageLogger/data/MessageLogger.cfi" 
  include "DiffractiveForwardAnalysis/Skimming/data/diffWToMuNu_SkimPaths.cff"
  include "DiffractiveForwardAnalysis/Skimming/data/diffWToMuNuOutputModule.cfi"
  replace diffWToENuOutputModule.fileName = "diffWToMuNu.root"

  untracked PSet options = {
    untracked bool wantSummary = true
  }

  untracked PSet maxEvents = { 
    untracked int32 input = 100 
  }

  source = PoolSource {
    untracked vstring fileNames = {
      "file:events.root"
    }
  }

  module evtInfo = AsciiOutputModule {
  }

  endpath end = { 
    evtInfo, diffWToMuNuOutputModule 
  }
}
